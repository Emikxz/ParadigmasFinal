// ========================= src/main.ts =========================
// Punto de entrada del programa.
// Solo llama al menú principal.

import { ejecutarMenuPrincipal } from "./io/menu";

function main(): void {
  ejecutarMenuPrincipal();
}

main();
